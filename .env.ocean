# Ocean Node Configuration
# Copy this file to .env.ocean.local and customize for your deployment

# CRITICAL: Change this private key for production use
# This key must start with 0x and be a valid Ethereum private key
OCEAN_PRIVATE_KEY=0x480fb4f905cc818cf4518a5bf4e1816246e4b1d0796fa317f0c2530d6bc2bedc

# Typesense Database Configuration
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
TYPESENSE_API_KEY=ocean-typesense-key

# Privacy and Access Control
# Comma-separated list of addresses authorized to decrypt data
AUTHORIZED_DECRYPTERS=0xb4e0370d8034b16cf29760b988c405d1ea95b9a0

# Comma-separated list of addresses authorized to publish data assets
AUTHORIZED_PUBLISHERS=0xb4e0370d8034b16cf29760b988c405d1ea95b9a0

# Comma-separated list of admin addresses with full access
ALLOWED_ADMINS=0xb4e0370d8034b16cf29760b988c405d1ea95b9a0

# Validate DDO signatures (recommended: true for production)
VALIDATE_UNSIGNED_DDO=true

# Security Configuration
# IP addresses or peer IDs to block
RATE_DENY_LIST=

# Rate limiting
MAX_REQ_PER_MINUTE=100
MAX_CONNECTIONS_PER_MINUTE=1000

# JWT Secret for authentication (change in production)
JWT_SECRET=ocean-jwt-secret-change-in-production

# Network Configuration
INTERFACES=HTTP,P2P
P2P_ENABLE_IPV4=true
P2P_ENABLE_IPV6=false

# Ocean Protocol Network (development, testnet, mainnet)
OCEAN_NETWORK=development

# Logging
LOG_LEVEL=info

# Ocean Node API URLs (adjust if running on different ports)
OCEAN_NODE_URL=http://localhost:8001
OCEAN_CONTROL_PANEL_URL=http://localhost:8001/controlpanel/

# Auto-publishing Configuration
# Enable automatic publishing of extracted memories as DataNFTs
OCEAN_AUTO_PUBLISH=true